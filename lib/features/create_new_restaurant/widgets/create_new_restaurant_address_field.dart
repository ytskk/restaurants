import 'package:flutter/material.dart';

class CreateNewRestaurantAddressField extends StatelessWidget {
  const CreateNewRestaurantAddressField({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
